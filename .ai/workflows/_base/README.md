# Base Workflows

This directory contains shared workflow structure.

## Files

| File | Purpose |
|---|---|
| `workflow_base.md` | Base workflow pattern used by workflow definitions. |

Workflow base files are reference material. The current runtime does not execute workflows.
