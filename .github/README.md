# GitHub Directory

This directory contains GitHub-related project configuration.

GitHub Copilot instructions are exposed through `.github/copilot-instructions.md`, which is an adapter that points to `.ai/rules/rules.md`.

The source of truth for shared rules is `.ai/rules/rules.md`.

Rules files must be normal files or directories. Do not use symbolic links.
