# Claude Directory

This directory contains Claude Code related project configuration and reusable Claude assets.

Project-wide rules are not stored here. Claude Code should start from the root `CLAUDE.md` adapter, then read `.ai/rules/rules.md` as the shared source of truth.

Rules files must be normal files or directories. Do not use symbolic links.
