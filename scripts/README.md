# Scripts Directory

This directory contains repository helper scripts.

## Current Scripts

| File | Purpose |
|---|---|
| `aios_inspect_smoke.ps1` | PowerShell smoke helper for AIOS inspection. |

Scripts should remain small and task-specific. Runtime behavior belongs in `src/aios/`.
